# ZED Box platform (Jetson Orin NX, no screen). The operator uses the web dashboard that viz_relay
# serves at http://<hostname>.local (install/install_dashboard_network.sh), so the
# LVGL window is off. Robot profiles for the box extend this instead of _jetson.
extends = "_jetson"

[ui]
enable = false

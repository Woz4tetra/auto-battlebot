# Required packages for auto-battlebot
# One package per line, lines starting with # are ignored

libsdl2-dev
python3-pip
python3-venv
python3-tk
python3-openssl
python3-dev
libwebsocketpp-dev
libjsoncpp-dev
libssl-dev
cmake
build-essential
libboost-all-dev
libeigen3-dev
unzip
wget
libpcl-dev
libffi-dev
valgrind
ripgrep

# clang-format, clang-tidy and clangd are NOT listed here. The unversioned
# packages differ per release and format the tree inconsistently, so they are
# pinned to .llvm-version by install/install_llvm_toolchain.sh instead.

{
    "tasks": [
        {
            "type": "shell",
            "label": "Build auto_battlebot",
            "command": "${workspaceFolder}/scripts/build.sh",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Build the project using CMake"
        }
    ],
    "version": "2.0.0"
}
"""Shared library modules for the calibration tools (apriltag_track.py, analyze_apriltag_mcap.py).

These are imported, not run directly: apriltag_detect (AprilTag detection + field-plane pose geometry)
and apriltag_mcap (the MCAP recording layout). The executable scripts stay one level up.
"""

#!/bin/bash
# USB-serial TUI: list / download / delete recordings, or live-stream sensors.
# Only works while the jig is idle (not recording).
BASE_DIR=$(realpath "$(dirname "$0")")
exec "${BASE_DIR}/../../../venv/bin/python" "${BASE_DIR}/console.py" "$@"

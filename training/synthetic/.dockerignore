# Local BlenderProc venv (~632MB); the image builds its own /opt/venv.
venv/

# Python caches and build artifacts (regenerated by pip install -e in the image).
__pycache__/
**/__pycache__/
*.py[cod]
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/
